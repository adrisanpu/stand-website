# URL de la app (stand-frontend) que inicia el checkout del plan Event (24€).
# Debe ser la ruta que llama a POST /v1/billing/checkout y redirige al usuario a Stripe (mismo flujo que el backend).
# No es una URL de Stripe; es la URL de la aplicación, p. ej. https://playstand.app/billing/checkout
VITE_STRIPE_EVENT_CHECKOUT_URL=
